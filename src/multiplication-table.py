def print_multiplication_table(table, start, end):
    for i in range(start, end + 1):
        print(f"{table} * {i} = {table * i}")

print_multiplication_table(5, 1, 10)

# Debugging
# step into           enters the block/function
# step over           next step
# step out            exits the current block/function

# tool windows: project, run, debug, expression (in debug)

# ctrl + b            go to method definition, or ctrl + click
# ctrl + space        autocomplete suggestions
# ctrl + j            code templates, select line(s) to wrap them

# alt + 1             project tool window
# alt + 4             run tool window
# alt + 6             to do tool window
# alt + 7             structure tool window

# ctrl + shift + a    search